# 快速开始

## 环境准备

确保已安装 Node.js 18+。

## 本地开发

```bash
# 安装依赖
npm install

# 启动开发服务器
npm run docs:dev
```

浏览器访问 `http://localhost:5173` 即可预览。

## 构建

```bash
npm run docs:build
```

构建产物位于 `dist/` 目录。
